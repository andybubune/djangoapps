apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: django-ingress
spec:
  rules:
    - host: 192.168.49.2
      http:
        paths:
          - backend:
              serviceName: django
              servicePort: 8001
            path: /polls
